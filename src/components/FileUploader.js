import React from 'react';

const FileUploader = () => {
    return (
        <div>
            <form>
                <input type="file" />
            </form>
        </div>
    );
};

export default FileUploader;
